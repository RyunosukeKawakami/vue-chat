export default class member{
    constructor(id, password, name, is_online) {
        this.id = id;
        this.password = password;
        this.name = name;
        this.is_online = is_online;
    }
}